version: '2'
services:
    postgres:
        image: jose/JSforumBD
        build: ./database
        container_name: bdJSforum
        ports:
          - "5434:5432" 
    web:
        image: jose/JSforumApp
        build: ./app
        container_name: appJSforum
        ports:
          - "8083:8080"
        links:
          - "postgres:host-banco" 
  
